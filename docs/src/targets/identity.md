# Target identity

A target is never architecture alone. SemASM models ISA, ABI, platform, object format, dialect, assembler, linker, execution profile, and machine model separately. See `semasm-target` and `ARCHITECTURE.md`.
