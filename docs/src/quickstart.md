# Quickstart

Build and run the CLI:

```bash
cargo run -p semasm-cli -- --version
cargo run -p semasm-cli -- status
```

End-to-end assembly examples will appear after the first hosted vertical slice (VS-02).
